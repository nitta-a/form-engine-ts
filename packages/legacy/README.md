# @form-engine-ts/legacy

Migration-only `answers` submission contracts for applications moving to the canonical `values` contract in
`@form-engine-ts/core`. New applications should depend on Core directly and should not install this package.
